import React from "react";

import "../styles/header.scss";

const Header = () => {
  const styles = {
    mainContainer: "ev-plan-choice__header__main-container"
  };
  return <div className={styles.mainContainer}>Header</div>;
};

export default Header;
